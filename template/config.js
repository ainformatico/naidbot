/**
 * This is configuration for connection.
 *
 * @property {string} username the username to connect to
 *
 * @property {string} password the password of the username
 *
 * @property {string} main_admin is the admin that has all the commands and can manage all the settings of naidbot
 *
 * */
var config =
{
  username    : '{user}',
  password    : '{password}',
  main_admin  : '{admin}'
};

module.exports = config;
